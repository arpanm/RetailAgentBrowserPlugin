/**
 * Smoke test to confirm Jest + bridge harness setup is working.
 * This is a placeholder until bridge protocol tests (W3-ext-bridge-tests) are added.
 */

describe("bridge smoke", () => {
  test("jest environment is ready", () => {
    expect(true).toBe(true);
  });
});

